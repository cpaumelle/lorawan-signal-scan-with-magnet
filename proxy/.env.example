# SiteScan proxy — no secrets needed for the proxy itself.
# The proxy forwards Microshare credentials at runtime (user enters them in the app).
# If you want to bind to a specific host/port, set these:
HOST=0.0.0.0
PORT=8001
